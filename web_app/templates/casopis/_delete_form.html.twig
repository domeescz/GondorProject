<form method="post" action="{{ path('app_casopis_delete', {'id': casopi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ casopi.id) }}">
    <button class="btn">Delete</button>
</form>
