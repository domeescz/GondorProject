<form method="post" action="{{ path('app_status_autor_delete', {'id': status_autor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ status_autor.id) }}">
    <button class="btn">Delete</button>
</form>
