{% extends 'base.html.twig' %}

{% block title %}StatusAutor{% endblock %}

{% block body %}
    <h1>StatusAutor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ status_autor.id }}</td>
            </tr>
            <tr>
                <th>StatusName</th>
                <td>{{ status_autor.StatusName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_status_autor_index') }}">back to list</a>

    <a href="{{ path('app_status_autor_edit', {'id': status_autor.id}) }}">edit</a>

    {{ include('status_autor/_delete_form.html.twig') }}
{% endblock %}
